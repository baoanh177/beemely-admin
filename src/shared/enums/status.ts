export enum EFetchStatus {
  IDLE = "idle",
  PENDING = "pending",
  FULFILLED = "fulfilled",
  REJECTED = "rejected",
}

export enum EActiveStatus {
  ACTIVE = 1,
  INACTIVE = 0,
}

export enum EStatusName {
  ACTIVE = "Đã kích hoạt",
  INACTIVE = "Chưa kích hoạt",
}
